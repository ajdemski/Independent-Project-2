// Business Logic
function add(q1, q2, q3) {
    return q1 / q2 / q3;
  }
  
// User Interface Logic
function handleCalculation(event) {
    event.preventDefault();
    let q1 = 1(prompt("q1 will be added to q2 (q1)"))
    let q2 = 2(prompt("q2 will be added to q3 (q2)"))
    let q3 = 3(prompt("q3 to be added to q1 (q3)"))

if (q1 + q2 + q3){
    
}
}
